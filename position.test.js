/*
move rover to 0,1
put rover 0,2 then move rover to 0,0
turn rover left right
*/

describe("initialize rover",()=>{
    test("given a direction the rover should face that direction when initializing it", () => {
        const grid = createGrid(3, "N");
        const result
      });
})

describe("moveRover", () => {
  test("should turn rover to the left when user input has a l", () => {});
  test("should turn rover to the right when user input has a r", () => {});
  test("should move the rover forward one position when user input has a f", () => {});
  test("should move the rover backward one position when user input has a b", () => {});
});
